<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal">

    <!-- LEFT SIDE: Camera & Overlays (Exact Match) -->
    <FrameLayout
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1">

        <com.pedro.library.view.OpenGlView
            android:id="@+id/surfaceView"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

        <RelativeLayout
            android:id="@+id/overlayContainer"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@android:color/transparent">
            
            <!-- आपका वेब ओवरले -->
            <WebView
                android:id="@+id/webOverlay"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:visibility="gone" />
                
            <!-- आपका ड्रैग होने वाला क्रिकेट स्कोरकार्ड (dragScoreboard) यहाँ पेस्ट करें -->
            
        </RelativeLayout>
    </FrameLayout>

    <!-- RIGHT SIDE: Control Panel for Buttons -->
    <ScrollView
        android:layout_width="350dp"
        android:layout_height="match_parent"
        android:background="#222222">
        
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">
            
            <!-- अपने सारे बटन (GO LIVE, Sign In, Text Inputs) यहाँ पेस्ट करें -->

        </LinearLayout>
    </ScrollView>

</LinearLayout>
